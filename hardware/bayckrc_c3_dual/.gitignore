dump_*.bin
parsed_*.txt
parsed_*.json
# parse.py's auto-generated reports live next to the dump as
# dump_NAME.parsed.{md,txt,json} - same rules (may contain operator
# secrets like encryption_key, brand, binding_rate).
dump_*.parsed.md
dump_*.parsed.txt
dump_*.parsed.json

# deep_scan.py reports (same privacy caveat as .parsed.md)
dump_*.deepscan.md
dump_*.deepscan.json

# Full checkout of ExpressLRS source tree (multi-MB; rebuildable from
# the tag recorded in vanilla_elrs_3.6.3/BUILD.md)
elrs_3_6_3_src/
